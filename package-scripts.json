{
  "name": "hrms-full-stack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run build && serve -s frontend/build\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start", 
    "ml": "cd ml-module && python simple_ml.py",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["hrms", "human-resource", "management", "mern", "machine-learning"],
  "author": "HRMS Development Team",
  "license": "ISC",
  "description": "Full-stack Human Resource Management System with ML capabilities",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
